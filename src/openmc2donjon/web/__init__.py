"""FastAPI backend for the openmc2donjon web UI.

Importing this subpackage requires the optional ``web`` extra
(``pip install -e ".[web]"``); ``server.create_app`` is the entry point
for the FastAPI application.
"""
